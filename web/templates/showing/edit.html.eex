<h2>Edit showing</h2>

<%= render "form.html", changeset: @changeset,
                        action: showing_path(@conn, :update, @showing),
                        theaters: @theaters,
                        movies: @movies %>

<%= link "Back", to: showing_path(@conn, :index) %>
